# 单元测试：H2 内存库，Flyway 关闭，@MockBean 提供 VectorStore 和 ChatClient
spring:
  datasource:
    url: jdbc:h2:mem:springclaw;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: false

springclaw:
  security:
    username: test
    password: test
